%li(id="#{user.id}")
  = user.full_name
  = link_to " Remove", remove_user_discussion_group_path(@group, :user_id => user.id),
   :method => :post, :remote => true, :class => "btn btn-danger btn-mini"
