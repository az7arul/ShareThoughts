%h4.author
  = message.user.full_name
  - if message.created_at.present?
    %small
      %acronym{rel: 'tooltip', title: message.created_at}
        = distance_of_time_in_words_to_now message.created_at
        ago
.well.well-small
  = simple_format message.content

